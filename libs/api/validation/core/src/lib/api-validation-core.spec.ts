import { apiValidationCore } from './api-validation-core';

describe('apiValidationCore', () => {
  it('should work', () => {
    expect(apiValidationCore()).toEqual('api-validation-core');
  });
});
