import { apiDocumentationCore } from './api-documentation-core';

describe('apiDocumentationCore', () => {
  it('should work', () => {
    expect(apiDocumentationCore()).toEqual('api-documentation-core');
  });
});
