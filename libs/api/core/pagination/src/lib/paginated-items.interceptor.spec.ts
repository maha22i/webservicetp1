import { PaginatedItemsInterceptor } from './paginated-items.interceptor';

describe('PaginatedItemsInterceptor', () => {
  it('should be defined', () => {
    expect(new PaginatedItemsInterceptor()).toBeDefined();
  });
});
