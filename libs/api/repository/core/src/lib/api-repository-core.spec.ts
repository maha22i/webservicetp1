import { apiRepositoryCore } from './api-repository-core';

describe('apiRepositoryCore', () => {
  it('should work', () => {
    expect(apiRepositoryCore()).toEqual('api-repository-core');
  });
});
